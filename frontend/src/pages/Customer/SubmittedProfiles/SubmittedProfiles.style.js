import styled from "styled-components";

export const SubmittedProfilesWrapper = styled.div`
  padding: 24px;
  background: #f8f9fd;
  min-height: 100vh;

  h2 {
    font-size: 24px;
    font-weight: 600;
    margin: 0 0 24px 0;
    color: #014c75;
  }

  .title-header {
    color: #014c75;
    margin-bottom: 24px;
  }

  .ant-table {
    background: white;
    border-radius: 8px;
  }
`;
